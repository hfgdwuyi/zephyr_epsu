# MCUboot bootloader v1.0.1 (OVERWRITE_ONLY layout, 2026-09-09)
# 升级策略：overwrite —— MCUboot 把 slot1 覆盖到 slot0。
# 无自动回滚，但升级失败可再次 DFU 重刷；连续升级稳定（无 swap 状态残留）。
CONFIG_BOOT_UPGRADE_ONLY=y
CONFIG_BOOT_SWAP_USING_SCRATCH=n
CONFIG_BOOT_SWAP_USING_OFFSET=n
# 静默 MCUboot 启动日志
CONFIG_MCUBOOT_LOG_LEVEL_OFF=y
